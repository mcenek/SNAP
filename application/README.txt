This folder is default php and codeigniter installation folder, later overwritten with SNAP interface files. Git repo only contains files that need to be over-installed to implement the SNAP web interface (do folder and document merge when pulling).

